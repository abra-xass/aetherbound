// Aetherbound — standalone game preview project, extracted from Thot.
// Game = original IP (Aspects, Echoforms, Atlas-based world). Tilesets/sprites
// are CC0 / CC-BY-licensed third-party packs (see docs/asset-credits.md).

plugins {
    alias(libs.plugins.android.application) apply false
    alias(libs.plugins.kotlin.android) apply false
    alias(libs.plugins.compose.compiler) apply false
}
